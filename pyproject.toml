[project]
name = "mnist-classifier"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.9.2",
    "torch>=2.4.1",
    "torchvision>=0.19.1",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
